# Intructions

// TODO

# Data:

```BASH
alias acfg='vi ~/.bash_aliases'
alias aucfg='source ~/.bash_aliases'
alias cfg='vi ~/.bashrc'
alias ucfg='source ~/.bashrc'

alias s='PORT=3001 npm start'
alias build='npm run build'
alias dev='npm run dev -- -p 3001'
alias vite-dev='npm run dev -- --port 3001'

alias cm='git add . && git commit -am'
alias tree='git log --graph'
alias ck='git checkout'
alias pushb='git push -u origin '
alias st='git status'
alias push='git push'
```

# aliases 
## theme: miloshadzic
```
alias cfg="vi ~/.zshrc"
alias save-cfg="source ~/.zshrc"


alias st="npm start"
alias lcl="npm run local"
alias i="npm install"
alias bld="npm run build"
alias beta="npm run build && npm publish --tag beta"
alias ilcl="npm install && npm run local"
alias ist="npm install & st"
alias clean-yalc="rm -r .yalc yalc.lock"
alias clean-npm="rm -r node_modules package-lock.json yarn.lock"

alias gp="git push --no-verify"
alias gpl="git pull"
alias gca="git add . && git commit -m "
alias gc="git commit -m "
alias ck="git checkout "
alias ckb="git checkout -"
```
